<form method="post" action="{{ path('app_token_rate_delete', {'id': token_rate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ token_rate.id) }}">
    <button class="btn btn-danger rounded-0 mb-1">Удалить</button>
</form>
