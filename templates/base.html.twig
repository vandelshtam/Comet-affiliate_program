<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>"> #}
        <link href="{{ asset ('https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css') }}" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="preconnect" href="{{ asset('https://fonts.googleapis.com') }}">
        <link rel="preconnect" href="{{ asset('https://fonts.gstatic.com') }}" crossorigin>
        <link rel="stylesheet" href="{{ asset('asset/css/normalize.css') }}">
        {# <link rel="stylesheet" href="{{ asset('asset/css/fonts2.css') }}"> #}
        {# <link rel="stylesheet" href="{{ asset('asset/css/fonts1.css') }}"> #}
        
        
        <link rel="stylesheet" href="{{ asset('asset/css/fonts.css') }}">
        
        
        {# <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/billboard.js/dist/billboard.min.css"/>
  <link rel=
"https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" type="text/css"/> #}
<link rel="stylesheet" href="{{ asset('asset/css/chief-slider.min.css') }}">
<link rel = "stylesheet"  href="{{ asset('asset/css/jquery.fancybox.css') }}">
<link rel="stylesheet" href="{{ asset('asset/css/style.css') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}
        {# {% block javascripts %}
            {{ include('js.html.twig') }}
        {% endblock %} #}
        

    </head>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">    
            <a class="nav-link" href="{{ path('app_home') }}">
                <img src="{{ asset('asset/img/icone-cometa-project.jpg') }}" alt="" class="logo" style="max-width: 50px;">
            </a>    
            <a class="navbar-brand" href="{{ path('app_home') }}">CoMeta Club</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse header_contacts" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {# <li class="nav-item">
                <a class="nav-link" href="tel:">+xx (xxx) xxx-xx-xx</a>
                </li> #}
                <li class="nav-item mr-3">
                <a class="nav-link header_email mr-1" href="mailto:ficakki48@gmail.com">comet-ap@gmail.com</a>
                </li>
                
                
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="ml-3"><a class="nav-link  header_exit ml-1" href="{{ path('app_logout') }}">Выйти</a></li>
                {% else %}
                <li class="ml-3"><a class="nav-link  header_signin ml-1" href="{{ path('app_login') }}">Войти</a></li>
                <li class="ml-3"><a class="nav-link pakage_table_name-1 ml-1" href="{{ path('app_register') }}">Регистрация</a></li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item ml-3">
                <a class="nav-link disabled pakage_table_name-1 ml-1" href="#" tabindex="-1" aria-disabled="true" href="{{ path('app_admin') }}">Вы Администратор</a>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item ml-3">
                <a class="nav-link disabled pakage_who ml-3" href="#" tabindex="-1" aria-disabled="true">Вы : {{ app.user.pesonalCode }}</a>
                </li>
                
                <li class="nav-item ml-3">
                {# <a class="btn btn-warning" href="#" tabindex="-1" aria-disabled="true">Быстрая консультация</a> #}
                <a data-fancybox data-src="#hidden-content" href="javascript:;" class="btn text-light rounded-0 header_send ml-1" style="background:  rgb(72, 109, 169);">Быстрое обращение в поддержку</a> 
                {% endif %}   
                </li>
            </ul>
            {# <form class="d-flex">
                <input class="form-control me-2" type="search" placeholder="Поиск" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Поиск</button>
            </form> #}
            </div>
        </div>
    </nav>
    <nav class="navbar navbar-expand-lg navbar-light" style="background:  rgb(72, 109, 169);">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent2" aria-controls="navbarSupportedContent2" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent2">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                <a class="nav-link active text-light" aria-current="page" href="{{ path('app_home') }}">Главная</a>
                </li>
                <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_about_project') }}">О проекте</a>
                </li>
                <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_condition') }}">Правила и акции</a>
                </li>
                {# <li class="nav-item">
                <a class="nav-link text-light" href="#">Блог</a>
                </li>
                <li class="nav-item">
                <a class="nav-link text-light" href="#">FAQ</a>
                </li>
                <li class="nav-item">
                <a class="nav-link text-light" href="#">Контакты</a>
                </li> #}
                <li class="nav-item">
                <a class="nav-link text-light" href="{{ path('app_table_pakage_index') }}">Обзор пакетов</a>
                </li>
                {# <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Пакеты
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_table_pakage_index') }}">Обзор пакетов</a></li>
                        <li><a class="dropdown-item" href="#">Starter</a></li>
                        <li><a class="dropdown-item" href="#">Basic</a></li>    
                        <li><a class="dropdown-item" href="#">Networker</a></li>
                        <li><a class="dropdown-item" href="#">Business</a></li>
                        <li><a class="dropdown-item" href="#">Trader</a></li>    
                        <li><a class="dropdown-item" href="#">VIP</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_pakege_new') }}">Купить пакет</a></li>
                    </ul>
                </li> #}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Кабинет пользователя
                    </a>
                    <ul class="dropdown-menu rounded-0" aria-labelledby="navbarDropdown" style="background: rgb(223, 230, 245);">
                        {# <li><a class="dropdown-item" href="{{ path('app_current_balance') }}">Текущий баланс</a></li> #}
                        <li><a class="dropdown-item fw-bold" href="{{ path('app_referral_network_myplace') }}" style="color: rgb(72, 109, 169);">Мой баланс</a></li>
                        <li><a class="dropdown-item fw-bold" href="{{ path('app_wallet_user') }}" style="color: rgb(72, 109, 169);">Мой кошелек</a></li>
                        {# <li><a class="dropdown-item" href="#">История транзакций</a></li> #}
                        {# {% if is_granted('ROLE_ADMIN') %} #}
                        <li><a class="dropdown-item fw-bold" href="{{ path('app_pakege_new',{'unique_code_get' : 'not' }) }}" style="color: rgb(72, 109, 169);">Купить пакет</a></li>
                            {% if app.user.pakageStatus == 1 %}
                            <li><a class="dropdown-item fw-bold" href="{{ path('app_pakege_edit_routing', {'id': app.user.pakageId}) }}" style="color: rgb(72, 109, 169);">Повысить пакет</a></li>
                            {% endif %}
                        {# {% else %}    #}
                            {# {% if app.user.pakageStatus == 0 %}
                            <li><a class="dropdown-item fw-bold" href="{{ path('app_pakege_new',{'unique_code_get' : 'not' }) }}" style="color: rgb(72, 109, 169);">Купить пакет</a></li>
                            {% elseif app.user.pakageStatus == 1  %}
                            <li><a class="dropdown-item fw-bold" href="{{ path('app_pakege_edit_routing', {'id': app.user.pakageId}) }}" style="color: rgb(72, 109, 169);">Повысить пакет</a></li> #}
                            {# {% endif %} #}
                        {# {% endif %}  #}
                        {# <li><a class="dropdown-item" href="{{ path('app_referral_network_index') }}">Участники реферальных сетей</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_list_referral_networks_index') }}">Реферальные сети</a></li> #}
                        {# <li><a class="dropdown-item" href="#">Вывод</a></li> #}
                        {# <li><a class="dropdown-item" href="#">Статистика (не активно)</a></li> #}
                        <li><hr class="dropdown-divider"></li>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {% if app.user.username == null %}
                        <li><a class="dropdown-item fw-bold" href="#" style="color: rgb(72, 109, 169);">Вы вошли как : {{ app.user.email }}</a></li>
                        {% else %}
                        <li><a class="dropdown-item fw-bold" href="#" style="color: rgb(72, 109, 169);">Вы вошли как : {{ app.user.username }}</a></li>
                        {% endif %} 
                        <li><a class="dropdown-item fw-bold" href="{{ path('app_personal_area') }}" style="color: rgb(72, 109, 169);">Личный кабинет</a></li>
                        {% else %}
                        <li><a class="dropdown-item fw-bold" href="#" style="color: rgb(72, 109, 169);">No login</a></li>
                        {% endif %} 
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        {# <li><a class="dropdown-item fw-bold" href="#" style="color: rgb(72, 109, 169);">Поддержка (не активно)</a></li> #}
                        {% if is_granted('ROLE_ADMIN') %}   
                        <li><a class="dropdown-item fw-bold" href="{{ path('app_admin') }}" style="color: rgb(72, 109, 169);">Кабинет администратора</a></li>
                        {% endif %} 
                        {% endif %} 
                    </ul>
                </li>
                {% endif %} 
                {# <li class="nav-item">
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
                </li> #}
            </ul>
            </div>
        </div>
    </nav>
    <body>
        {% block body %}
         
        {% endblock %}
        {% block footer %}
        {{ include('footer.html.twig') }}
        {% endblock %}
        {% block fast_consultation %}
            {# {% if form_errors(fast_consultation_form) %}
                <div class="my-custom-class-for-errors">
                    {{ form_errors(fast_consultation_form) }}
                </div>
            {% endif %} #}
            {{ include('fast_consultation.html.twig') }}
    {% endblock %}
    </body>
    
    
    {% block javascripts %}
            {{ include('js.html.twig') }}
    {% endblock %}
    {% block javascripts2 %}      
    {% endblock %}
    {% block javascripts3 %}      
    {% endblock %}
    {% block javascripts4 %}      
    {% endblock %}
    {% block javascripts_three %}      
    {% endblock %}
    {% block javascripts_reverse_counter %}
    {% endblock %}
    {% block agree_terms %}
    {% endblock %}
</html>
