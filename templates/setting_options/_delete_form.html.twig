<form method="post" action="{{ path('app_setting_options_delete', {'id': setting_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting_option.id) }}">
    <button class="btn btn-danger rounded-0 mb-1">Delete</button>
</form>
