{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="container-xxl">

    {# {% if user.PersonalDataId == NULL %}
    <div class="alert alert-warning" role="alert">
    Важная информация:
        <ul>
            <li>Вы обязательно должны заполнить персональные данные и принять условия нашей платформы. Без этого вы не сможете совершать операции на нашей платформе. Спасибо за понимание! <code><a href="{{ path('app_personal_data_new', {'user_id': user.id}) }}">перейти к заполнению</a></code></li>
        </ul>
    </div>
    {% endif %} #}
    {% for label, messages in app.flashes(['success', 'notice']) %}
        {% for message in messages %}
            <div class="alert alert-success" role="alert">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
    {% for label, messages in app.flashes(['info', 'notice']) %}
        {% for message in messages %}
            <div class="alert alert-info" role="alert">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
  <div class="example-wrapper">
      <h1>{{ controller_name }}</h1>
  </div>
  <div class="accordion accordion-flush" id="accordionExample">
    <div class="accordion-item">
      <h2 class="accordion-header" id="headingOne">
        <button class="accordion-button link-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
          Информация о проекте 
        </button>
      </h2>
      <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
        <div class="accordion-body">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
              <a class="nav-link link-secondary" href="https://miro.com/app/board/uXjVOHUMn6U=/">Mindmap проекта</a>
              </li>
              <li class="nav-item">
              <a class="nav-link link-secondary" href="">Персональные данные</a>
              </li>
          </ul> 
        </div>
      </div>
    </div>
    <div class="accordion-item mb-5">
      <h2 class="accordion-header" id="headingFour">
        <button class="accordion-button link-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFour" aria-expanded="false" aria-controls="collapseFour">
          резервный блок 
        </button>
      </h2>
      <div id="collapseFour" class="accordion-collapse collapse" aria-labelledby="headingFour" data-bs-parent="#accordionExample">
        <div class="accordion-body">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
              <a class="nav-link link-secondary" href="">резерв</a>
              </li>
              <li class="nav-item">
              <a class="nav-link link-secondary" href="">резерв</a>
              </li>
              <li class="nav-item">
              <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Резерв</a>
              </li>
          </ul> 
        </div>
      </div>
    </div>
  </div>
  <div class="card mb-3">
        <img src="{{ asset('asset/img/cometa-coin-1.jpeg') }}" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">Проект "Co-Meta point"</h5>
            <p class="card-text">Этот проект является электронной площадкой для пользователей, где они могут покупать, обменивать, продавать криптовалюту, локальные токены проекта, а так же товары и услуги.</p>
            <p class="card-text"><small class="text-muted">Опубликовано 16 марта 2022 года</small></p>
        </div>
        </div>
  </div>
</div>

{% endblock %}
