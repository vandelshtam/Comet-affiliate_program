<form method="post" action="{{ path('app_pakege_delete', {'id': pakege.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pakege.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
