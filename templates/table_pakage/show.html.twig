{% extends 'base.html.twig' %}

{% block title %}TablePakage{% endblock %}

{% block body %}
    <h1>TablePakage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table_pakage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ table_pakage.name }}</td>
            </tr>
            <tr>
                <th>Price_pakage</th>
                <td>{{ table_pakage.pricePakage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_table_pakage_index') }}">back to list</a>

    <a href="{{ path('app_table_pakage_edit', {'id': table_pakage.id}) }}">edit</a>

    {{ include('table_pakage/_delete_form.html.twig') }}
{% endblock %}
