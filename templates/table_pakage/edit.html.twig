{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="pb-3 pt-3 mb-0" style="background: rgb(223, 230, 245);">
    <div class="example-wrapper container-xxl bg-white container-xxl col-sm-6  shadow p-3 mb-1 mt-3 rounded-0">
        <h1 class="d-flex justify-content-center  mb-5" style="color: rgb(72, 109, 169);">{{ controller_name }}</h1>
        {% if is_granted('ROLE_ADMIN') %}   
        {{ include('table_pakage/_form.html.twig', {'button_label': 'Обновить'}) }}
        {% endif %}
        <a class="btn btn-primary rounded-0 mb-1" href="{{ path('app_table_pakage_index') }}">Назад</a>
        {% if is_granted('ROLE_ADMIN') %}   
        {{ include('table_pakage/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>
{% endblock %}
