<form method="post" action="{{ path('app_referral_network_delete', {'id': referral_network.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ referral_network.id) }}">
    <button class="btn btn-danger rounded-0 mb-1">Удалить</button>
</form>
