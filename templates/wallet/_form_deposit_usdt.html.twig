{{ form_start(form) }}
    {{ form_row(form.usdt) }}<div id="emailHelp" class="form-text mb-3">В форме отобразился текущий баланс кошелька USDT. Введите сумму USDT которую хотите внести  на кошелек </div> 
    <div class="hidden">
      {{ form_row(form.user) }}
    </div>
    <button class="btn btn-primary rounded-0 mb-1">{{ button_label|default('Подтвердить') }}</button>
{{ form_end(form) }}
